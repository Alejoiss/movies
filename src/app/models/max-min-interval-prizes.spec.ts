import { MaxMinIntervalPrizes } from './max-min-interval-prizes';

describe('MaxMinIntervalPrizes', () => {
    it('should create an instance', () => {
        expect(new MaxMinIntervalPrizes()).toBeTruthy();
    });
});
